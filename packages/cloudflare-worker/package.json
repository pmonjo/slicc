{
  "name": "@slicc/cloudflare-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx wrangler deploy --dry-run --outdir dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p ../../tsconfig.worker.json",
    "dev": "npx wrangler dev --config wrangler.jsonc"
  },
  "dependencies": {
    "@slicc/cloud-core": "*",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260520.1",
    "wrangler": "4.93.0"
  }
}
