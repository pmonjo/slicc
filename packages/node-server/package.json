{
  "name": "@slicc/node-server",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --input-type=module -e \"import { rmSync } from 'node:fs'; rmSync('../../dist/node-server', { recursive: true, force: true });\" && tsc -p ../../tsconfig.cli.json && node scripts/inline-shared.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p ../../tsconfig.cli.json"
  },
  "dependencies": {
    "@slicc/shared-ts": "*"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/ws": "^8.5.14",
    "electron": "41.5.1"
  }
}
